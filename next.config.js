// next.config.js
/** @type {import('next').NextConfig} */
module.exports = {
  // static export
  output: 'export',
  // PROJECT-PAGE path (must match your repo name exactly)
  basePath: '/SDBlogs',
  assetPrefix: '/SDBlogs/',
  trailingSlash: true,
}
